// JavaScript program to redirect to a specified URL.
function redirectTo(url) {
    // Redirect to the specified URL
    window.location.href = url;
}

// Example usage:
let url = "https://www.example.com";
console.log("Redirecting to:", url);
redirectTo(url);
